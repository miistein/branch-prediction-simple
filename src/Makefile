# make sure to change gcc-5 to gcc when turning in
# https://stackoverflow.com/questions/172587/what-is-the-difference-between-g-and-gcc
CC=g++
OPTS=-std=c++11 -Wall

all:
	$(CC) $(OPTS) *.cpp -o predictor

clean:
	rm -f *.o predictor;
